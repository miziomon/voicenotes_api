# =============================================================================
# FILE DI ESEMPIO - CONFIGURAZIONE AMBIENTE
# =============================================================================
# Copia questo file in .env e sostituisci i valori con quelli reali.
# Il file .env non viene committato in git (è in .gitignore).
# =============================================================================

# =============================================================================
# CONFIGURAZIONE SUPABASE
# =============================================================================
# URL del progetto Supabase
SUPABASE_URL=https://your-project.supabase.co

# Chiave API Supabase (anon/public key)
SUPABASE_KEY=your-supabase-anon-key

# Chiave Service Role Supabase (per il proxy - bypassa RLS)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# CONFIGURAZIONE SUPABASE PROXY
# =============================================================================
# Whitelist tabelle (lasciare vuoto per permettere tutte le tabelle)
# Formato: lista separata da virgole (es: notes,chat_sessions,chat_messages)
PROXY_TABLES_WHITELIST=

# Blacklist tabelle (lasciare vuoto per non bloccare nessuna tabella)
# Formato: lista separata da virgole (es: internal_logs,system_config)
PROXY_TABLES_BLACKLIST=

# Metodi HTTP bloccati (raccomandato: lasciare vuoto o specificare metodi pericolosi)
# Formato: lista separata da virgole (es: TRUNCATE,DROP,ALTER)
PROXY_BLOCKED_METHODS=

# =============================================================================
# CONFIGURAZIONE OPENAI
# =============================================================================
# Chiave API OpenAI (opzionale, per embedding OpenAI)
OPENAI_API_KEY=sk-your-openai-api-key

# =============================================================================
# CONFIGURAZIONE GOOGLE GEMINI
# =============================================================================
# Chiave API di Google Gemini (trovala su aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key

# =============================================================================
# CONFIGURAZIONE TEST
# =============================================================================
# User ID per i test API (usato in api_tests.http con REST Client)
# Sostituisci con un UUID reale del tuo database
TEST_USER_ID=00000000-0000-0000-0000-000000000000
